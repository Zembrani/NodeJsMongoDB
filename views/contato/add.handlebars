{{#each error}}
<div class="alert alert-danger">{{text}}</div>
{{else}}

{{/each}}

<h3>Novo Contato</h3>
<div class="card">
  <div class="card-body">
    <form action="/contato/add/new" method="POST">
      <label for="name">Número ou email:</label>
      <input type="text" id="contato" name="contato" placeholder="Número ou email" class="form-control">
      <br>
      <button type="submit" class="btn btn-success">Adicionar contato</button>
    </form>
  </div>
</div>