{{#each error}}
<div class="alert alert-danger">{{text}}</div>
{{else}}

{{/each}}

{{#each contatovar}}
<h3>Alterar Contato</h3>
<div class="card">
  <div class="card-body">
    <form action="/contato/update" method="POST">
      <input type="hidden" id="id" name="id" value="{{id}}">
      <label for="name">Número ou email:</label>
      <input type="text" id="contato" name="contato" class="form-control" value="{{contato}}">
      <br>
      <button type="submit" class="btn btn-success">Editar contato</button>
    </form>
  </div>
</div>
{{/each}}